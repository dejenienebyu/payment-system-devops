apiVersion: v1
kind: Service
metadata:
  name: payment-api-service
  namespace: default
spec:
  selector:
    app: payment-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: LoadBalancer
